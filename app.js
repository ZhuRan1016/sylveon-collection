let cards=[],era='全部';const owned=new Set(JSON.parse(localStorage.getItem('sylveon-owned')||'[]'));const key=c=>`${c.name}|${c.number}|${c.set}`;const save=()=>localStorage.setItem('sylveon-owned',JSON.stringify([...owned]));async function init(){cards=await fetch('cards.json').then(r=>r.json());render()}function render(){const q=document.querySelector('#q').value.toLowerCase(),f=document.querySelector('#filter').value;const eras=['全部',...new Set(cards.map(c=>c.era))];document.querySelector('#eras').innerHTML=eras.map(e=>`<button class="era ${e===era?'active':''}" data-e="${e}">${e}</button>`).join('');document.querySelectorAll('.era').forEach(b=>b.onclick=()=>{era=b.dataset.e;render()});const arr=cards.filter(c=>{const has=owned.has(key(c));return(!q||`${c.name} ${c.number} ${c.set}`.toLowerCase().includes(q))&&(era==='全部'||c.era===era)&&(f==='all'||(f==='owned'&&has)||(f==='missing'&&!has))});document.querySelector('#grid').innerHTML=arr.map(c=>{const has=owned.has(key(c));return`<article class="card ${has?'owned':''}"><div class="img">${c.image?`<img loading="lazy" src="${c.image}">`:''}<div class="placeholder">🎀<br>${c.name}<br><small>${c.number}</small></div><button class="check" data-k="${encodeURIComponent(key(c))}">${has?'✓':'＋'}</button></div><div class="body"><b>${c.name}</b><div class="meta">${c.number}<br>${c.set}</div></div></article>`}).join('');document.querySelectorAll('.check').forEach(b=>b.onclick=()=>{const k=decodeURIComponent(b.dataset.k);owned.has(k)?owned.delete(k):owned.add(k);save();render()});const n=cards.filter(c=>owned.has(key(c))).length,p=cards.length?Math.round(n/cards.length*100):0;document.querySelector('#stats').textContent=`已收集 ${n} / ${cards.length}（${p}%）`;document.querySelector('#bar').style.width=p+'%'}document.querySelector('#q').oninput=render;document.querySelector('#filter').onchange=render;document.querySelector('#exportBtn').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify({owned:[...owned]},null,2)],{type:'application/json'}));a.download='sylveon-backup.json';a.click()};document.querySelector('#importFile').onchange=async e=>{const d=JSON.parse(await e.target.files[0].text());owned.clear();(d.owned||[]).forEach(x=>owned.add(x));save();render()};if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');init();